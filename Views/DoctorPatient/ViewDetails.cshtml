@model PracticeFlow.Models.User

@{
    ViewData["Title"] = "Patient Details";
}

<div class="container mt-4">
    <h2>Patient Details</h2>
    <hr />

    <div class="card">
        <div class="card-body">
            <div class="row mb-3">
                <div class="col-md-3"><strong>Full Name:</strong></div>
                <div class="col-md-9">@Model.FullName</div>
            </div>
            <div class="row mb-3">
                <div class="col-md-3"><strong>Username:</strong></div>
                <div class="col-md-9">@Model.Username</div>
            </div>
            <div class="row mb-3">
                <div class="col-md-3"><strong>Email:</strong></div>
                <div class="col-md-9">@Model.Email</div>
            </div>
            <div class="row mb-3">
                <div class="col-md-3"><strong>Date of Birth:</strong></div>
                <div class="col-md-9">
                    @if (Model.DateOfBirth.HasValue)
                    {
                        @Model.DateOfBirth.Value.ToString("MMMM dd, yyyy")
                    }
                    else
                    {
                        <span class="text-muted">Not provided</span>
                    }
                </div>
            </div>
            <div class="row mb-3">
                <div class="col-md-3"><strong>Patient ID:</strong></div>
                <div class="col-md-9">@Model.UserID</div>
            </div>
        </div>
    </div>


<div class="mt-3">
    <a asp-action="MedicalHistory" asp-route-id="@Model.UserID" class="btn btn-primary">View Medical History</a>
    <a asp-action="ViewVitals" asp-route-id="@Model.UserID" class="btn btn-success">View Vitals History</a>
    <a asp-action="ViewPatients" class="btn btn-secondary">Back to Patients</a>
</div>